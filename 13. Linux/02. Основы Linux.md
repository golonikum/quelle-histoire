# Основы Linux

## Root и sudo

В Debian дистрибутивах (Ubuntu) отключен root, и доступ к нему можно получить с помощью команды `sudo`.

Чтобы посмотреть есть ли у пользователя права root:

```bash
sudo whoami
```

Если вы видите `root` в ответе, то у вас есть права root.

## Bash (Bourne-Again SHell)

Баш - это интерактивная командная строка. В ней можно вводить команды и получать на них ответ.

- `Ctrl + A` - переход на начало строки (также кнопка `Home`)
- `Ctrl + U` - удаление текущей строки
- `Ctrl + C` - прервать выполнение команды
- `;` - разделитель команд

### history

```bash
$ history
    1  ls
    2  tty
    3  sudo openvt -sc 18 /bin/bash
    4  tty
    5  sudo openvt -fsc 18 /bin/bash
    6  tty
    7  who
    8  sudo who
    9  ls -a
   10  ls -l
   11  history
   12  tty
   13  ls -a
   14  history
   15  ls -a
   16  ls -l
   17  history
$ !10
ls -l
total 36
drwxr-xr-x 3 aniketos aniketos 4096 May 29 15:09 Desktop
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Documents
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Downloads
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Music
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Pictures
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Public
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Templates
drwxr-xr-x 2 aniketos aniketos 4096 Apr 29 13:14 Videos
drwx------ 6 aniketos aniketos 4096 May 28 19:27 snap
```

### tty

tty - это терминал. В Linux есть несколько терминалов, и каждый из них может быть активен или неактивен. Чтобы узнать, какой терминал активен:

```bash
tty
```

Чтобы переключиться на другой терминал, надо нажать `Ctrl + Alt + F1`, `Ctrl + Alt + F2`, ..., `Ctrl + Alt + F6`. В Ubuntu 1 и 2 терминалы графические, остальные текстовые.

Вот так можно создать новый терминал, куда можно перейти с помощью `Ctrl + Alt + F12`:

```bash
sudo openvt -c 12 login
```

А так можно создать новый терминал, и сразу переключиться в него:

```bash
sudo openvt -sc 13 /bin/bash
```

Чтобы вернуться на существующий 13-ый терминал:

```bash
sudo chvt 13
```

### apt

**apt** - это пакетный менеджер для Debian-дистрибутивов. С его помощью можно устанавливать, удалять и обновлять пакеты.

Чтобы установить пакет:

```bash
sudo apt install <package_name>
```

Чтобы удалить пакет:

```bash
sudo apt remove <package_name>
```

## Доступ по SSH (Secure Shell)

Чтобы можно было подключаться к машине по SSH, нужно установить и запустить ssh-server:

```bash
sudo apt update
sudo apt install openssh-server
sudo systemctl enable --now ssh
```

Теперь надо узнать IP-адрес машины:

```bash
ip a
```

И подключиться к машине через клиент Putty.

Проверка статуса:

```bash
systemctl status sshd
```

### ls

ls - это команда для просмотра содержимого директории.

- `ls` - список файлов в текущей директории
- `ls -a` - список файлов в текущей директории, включая скрытые файлы
- `ls -l` - список файлов в текущей директории, включая права доступа, владельца и размер файла
- `ls -lh` - список файлов в текущей директории, включая права доступа, владельца и размер файла в читаемом виде (например, 4.0K, 1.0M)

```bash
$ ls -l /
total 4003916
lrwxrwxrwx   1 root root          7 Apr 20 11:46 bin -> usr/bin
drwxr-xr-x   3 root root       4096 Apr 29 10:32 boot
dr-xr-xr-x   2 root root       4096 Apr 23 05:18 cdrom
drwxr-xr-x  19 root root       4360 May 29 17:07 dev
drwxr-xr-x 146 root root      12288 May 29 16:16 etc
drwxr-xr-x   3 root root       4096 Apr 29 10:27 home
lrwxrwxrwx   1 root root          7 Apr 20 11:46 lib -> usr/lib
lrwxrwxrwx   1 root root          9 Apr 20 11:46 lib64 -> usr/lib64
drwx------   2 root root      16384 Apr 29 10:16 lost+found
drwxr-xr-x   2 root root       4096 Apr 23 04:15 media
drwxr-xr-x   2 root root       4096 Apr 23 04:15 mnt
drwxr-xr-x   2 root root       4096 Apr 23 04:15 opt
dr-xr-xr-x 248 root root          0 May 29 16:56 proc
drwx------   5 root root       4096 Apr 29 11:04 root
drwxr-xr-x  46 root root       1120 May 29 17:43 run
lrwxrwxrwx   1 root root          8 Apr 20 11:46 sbin -> usr/sbin
drwxr-xr-x  15 root root       4096 Apr 23 04:22 snap
drwxr-xr-x   2 root root       4096 Apr 23 04:15 srv
-rw-------   1 root root 4099932160 Apr 29 10:24 swap.img
dr-xr-xr-x  13 root root          0 May 29 21:31 sys
drwxrwxrwt  17 root root        380 May 29 21:25 tmp
drwxr-xr-x  12 root root       4096 Apr 23 04:15 usr
drwxr-xr-x  14 root root       4096 Apr 29 11:04 var
```

## Файловая система

`VFS` - это виртуальная файловая система. Она позволяет использовать несколько файловых систем одновременно.

Процесс связывания файловой системы с устройством в Linux называется монтированием (`mounting`). Для подключения файловой системы к существующей иерархии файловых систем (корню) используется команда `mount`.

**Виртуальная файловая система (англ. virtual file system — VFS**) или виртуальный коммутатор файловой системы (англ. `virtual filesystem switch`) — служит интерфейсом между ядром и конкретной файловой системой, позволяя легко добавлять поддержку новых типов файловых систем, внося изменения только в ядро операционной системы:

![VFS](images/02.%20VFS.png)

**FUSE (англ. filesystem in userspace)** — модуль ядра, который позволяет создавать новые типы файловых систем, доступные для монтирования пользователями без привилегий за счёт запуска кода файловой системы в пользовательском пространстве при этом не прибегая к программированию на уровне ядра.

### Файловая система /udev

Под `udev` понимается реализация файловой системы устройств `devfs`.

`udev` обеспечивает все необходимые средства для динамического создания и удаления файлов устройств и символических ссылок в каталоге `/dev`.

Содержимое каталога `/dev` хранится на виртуальной файловой системе, и все файлы, находящиеся в нём, создаются при каждом запуске системы.

### Файловая система /proc

Файловая система `/proc` содержит каталоги и виртуальные файлы. Виртуальный файл предоставляет полученную из ядра информацию, а такжеможет использоваться для передачи в ядро пользовательской информации. Большинство программ получают информацию из файлов в `/proc`, например, `top` и `ps`.

`/proc` также источник информации об аппаратных средствах.

- `/proc/cpuinfo`
  Здесь вы можете получить подробную информацию о процессорах.
- `/proc/[pid]`
  Имя каждого подкаталога соответствует PID (идентификатору процесса) работающего в системе процесса.
- `/proc/filesystems`
  Этот файл содержит перечень всех поддерживаемых ядром типов файловых систем. Строки, начинающиеся с 'nodev', указывают на то, что ФС не является «физической».
- `/proc/cmdline`
  В этом файле содержатся все аргументы, переданные ядру в момент старта системы.
- `/proc/loadavg`
  Хранит информацию о загрузке системы. Первые три поля хранят ту же информацию, которую вы получаете о средней загрузке системы при помощи команды `uptime`.

  Четвёртое поле состоит из двух значений, разделённых слэшем. Первая часть значения поля показывает количество выполняющихся в данный момент процессов/потоков. Это значение не может быть больше количества присутствующих в вашей системе CPU. Вторая часть поля отображает количество процессов присутствующих в системе.

  Значение пятого поля содержит ID последнего запущенного в системе процесса.

- `/proc/meminfo`
  Здесь хранится информация об использовании системой памяти.
- `/proc/sys`
  Подкаталог `/proc/sys` позволяет изменять некоторые параметры ядра в реальном режиме времени или отображать их.
  Например `/proc/sys/kernel/hostname`.
  В этом файле содержится назначенное системе имя.

### Основные разделы файловой системы

| Папка    | Назначение                           | Примеры файлов / содержимого                               |
| :------- | :----------------------------------- | :--------------------------------------------------------- |
| `/`      | Корень файловой системы              | Все остальные каталоги                                     |
| `/bin`   | Базовые команды                      | `ls`, `cp`, `mv`, `cat`, `bash`                            |
| `/sbin`  | Админские утилиты                    | `ip`, `fsck`, `mount`, `reboot`                            |
| `/boot`  | Файлы загрузки                       | `vmlinuz`, `initrd.img`, `grub/`                           |
| `/dev`   | Устройства                           | `sda`, `tty`, `null`, `random`                             |
| `/etc`   | Конфиги системы                      | `passwd`, `hosts`, `ssh/sshd_config`, `fstab`              |
| `/home`  | Домашние папки пользователей         | `/home/nikita/`, документы, загрузки, настройки приложений |
| `/root`  | Домашняя папка root                  | Конфиги root, shell history                                |
| `/lib`   | Библиотеки для программ              | `libc.so`, модули ядра                                     |
| `/usr`   | Пользовательские программы и ресурсы | `usr/bin/`, `usr/lib/`, `usr/share/`                       |
| `/var`   | Изменяемые данные                    | `log/`, `cache/`, `spool/`, базы данных сервисов           |
| `/tmp`   | Временные файлы                      | Временные файлы программ, кэши на время работы             |
| `/proc`  | Виртуальная инфа о системе           | `cpuinfo`, `meminfo`, папки процессов `1234/`              |
| `/sys`   | Инфо о ядре и устройствах            | Параметры устройств и драйверов                            |
| `/mnt`   | Временное монтирование               | Вручную подключённые диски или разделы                     |
| `/media` | Съёмные носители                     | Флешки, внешние диски, CD/DVD                              |
| `/opt`   | Дополнительное ПО                    | Сторонние программы, например отдельные пакеты             |
| `/srv`   | Данные сервисов                      | Файлы web-, ftp- и других сервисов                         |

### df (report file system space usage)

```bash
$ df -TH
Filesystem     Type   Size  Used Avail Use% Mounted on
tmpfs          tmpfs  713M  6.3M  707M   1% /run
/dev/sda2      ext4    27G   12G   14G  46% /
tmpfs          tmpfs  1.8G     0  1.8G   0% /dev/shm
none           tmpfs  1.1M     0  1.1M   0% /run/credentials/systemd-journald.service
tmpfs          tmpfs  1.8G  8.2k  1.8G   1% /tmp
tmpfs          tmpfs  357M   62k  357M   1% /run/user/60578
none           tmpfs  1.1M     0  1.1M   0% /run/credentials/getty@tty5.service
tmpfs          tmpfs  357M   54k  357M   1% /run/user/1000
none           tmpfs  1.1M     0  1.1M   0% /run/credentials/systemd-resolved.service
none           tmpfs  1.1M     0  1.1M   0% /run/credentials/getty@tty4.service
none           tmpfs  1.1M     0  1.1M   0% /run/credentials/getty@tty2.service
none           tmpfs  1.1M     0  1.1M   0% /run/credentials/getty@tty6.service
```

```bash
$ df -TH -x tmpfs
Filesystem     Type  Size  Used Avail Use% Mounted on
/dev/sda2      ext4   27G   12G   14G  46% /
```

## du (Estimate file space usage)

```bash
$ du -sh /home/aniketos
160M    /home/aniketos
```

## Выключение и перезагрузка

### poweroff

```bash
$ poweroff
```

### shutdown

```bash
$ shutdown -h 19:30 [сообщение]
```

```bash
$ shutdown -h now
```

Для перезагрузки:

```bash
$ shutdown -r now
```

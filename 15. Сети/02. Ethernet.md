# Ethernet

Ethernet — это **базовая технология проводной локальной связи**, которая обеспечивает передачу битов по физической среде и организацию кадрового обмена на канальном уровне OSI.

![Etherner определение](images/02.%20Ethernet%20определение.png)

## Место в модели OSI

В строгом смысле `Ethernet` не является только «физическим уровнем»: он охватывает и **физический уровень (L1)**, и **канальный уровень (L2)** модели OSI.

На физическом уровне `Ethernet` определяет способы представления битов в среде передачи, электрические или оптические параметры сигнала, синхронизацию, кодирование и характеристики линий связи.

На канальном уровне он задаёт формат кадров, MAC-адресацию и правила взаимодействия узлов в локальной сети.

## Физическая сущность

В классическом понимании `Ethernet` — это технология передачи данных по среде, где биты преобразуются в сигнал, распространяющийся по кабелю или оптическому волокну.

Исторически ранние варианты `Ethernet` были построены как общая среда передачи (общая шина), то есть несколько станций делили один сегмент и конкурировали за доступ к нему. Передача велась в режиме `half-duplex` на скорости до 10 Мбит/с.

Позднее архитектура изменилась: появились коммутируемые сети, где каждое устройство подключается к отдельному порту концентратора (в будущем коммутатора), а общая среда и коллизии ушли в прошлое.

`Ethernet` стандартизует не «интернет» и не прикладные протоколы, а нижний слой локальной связи: электрические/оптические параметры, форматы кадров, принципы адресации и совместимость оборудования.

Именно поэтому Ethernet-соединение может работать с разными сетевыми протоколами верхних уровней, например с `IP`, не будучи само по себе IP-протоколом.

## Витая пара

Для подключения компьютера к коммутатору по кабелю витой пары используются коннекторы **RJ-45**. Таких коннекторов вам понадобится в два раза больше, чем компьютеров, поскольку каждый отрезок кабеля нужно оснастить коннекторами с обоих концов. Но мы рекомендуем покупать коннекторы с запасом, поскольку во время обжатия кабеля они могут быть повреждены.

![Витая пара - прямой кабель](images/02.%20Витая%20пара%20-%20прямой%20кабель.png)
![Витая пара - перекрестный кабель](images/02.%20Витая%20пара%20-%20перекрестный%20кабель.png)

Для передачи сигналов на линиях связи 100 Мбит/с используются только две пары из четырех, имеющихся в кабеле (в Gigabit Ethernet на скорости 1000 Мбит/с задействованы уже все четыре пары). Хотя это и не предусмотрено стандартами, администратор может на свое усмотрение задействовать оставшиеся пары — например, использовать средние провода (пару голубой — бело-голубой) под телефон, а коричневую пару — в качестве замены одной из сигнальных при обнаружении обрыва в кабеле. При этом следует учитывать, что для питания устройств поверх сети Ethernet (Power over Ethernet, PoE) служит центральная пара проводов (см. о РоЕ подробнее чуть далее) и что в гигабитных каналах задействованы все провода.

Два компьютера можно объединить в сеть и без коммутатора. Для этого используется перекрестная обжимка кабеля (**crossover**). В этом случае на одной из сторон кабеля меняются местами входные и выходные провода — первая и третья пары (табл. 3.4). Перекрестная обжимка может понадобиться не только при соединении одного компьютера с другим, но и при подключении ADSL-модема непосредственно к компьютеру или при соединении двух коммутаторов ранних моделей выпуска.

Современные модели коммутаторов обладают возможностью определения типа расшивки кабеля и автоматически переключаются на нужный вариант. У ранее выпускавшихся моделей этой функции нет, поэтому, например, для соединения двух таких устройств между собой также необходимо использовать crossover-кабель. На некоторых моделях имеется либо кнопка переключения типа одного из портов, либо два разъема для одного порта, соответствующих тому или иному варианту подключения (называемых `MDI` и `MDIX`).

![Витая пара - прямой vs перекрестный кабель](images/02.%20Витая%20пара%20-%20правила%20соединения%20устройств.png)

## Кадры и адресация

На канальном уровне `Ethernet` передаёт данные в виде **кадров**, внутри которых находятся MAC-адреса источника и назначения, а также служебные поля для контроля корректности передачи.

`MAC-адрес` нужен для доставки кадра в пределах локального сегмента или через коммутаторы, а не для маршрутизации между разными сетями — это уже задача сетевого уровня.

## Правила

![Физические правила](images/02.%20Физические%20правила.png)

![Логические правила](images/02.%20Логические%20правила%201-3.png)
![Логические правила](images/02.%20Логические%20правила%204.png)

## Эволюция технологии

`Ethernet` начинался с коаксиальных сегментов и шинной организации сети, затем перешёл к витой паре, коммутаторам и более высоким скоростям передачи.

Сейчас под `Ethernet` обычно подразумевают целое семейство стандартов от медных линий до оптики, от 10 Мбит/с до гигабитных и более скоростных вариантов.

При этом общая идея остаётся прежней: обеспечить стандартизированный, совместимый и относительно простой способ обмена данными между узлами локальной сети.

## Стандарты Ethernet

| Стандарт     |   Скорость | Среда / кабель                       |                                        Типичное расстояние |
| :----------- | ---------: | :----------------------------------- | ---------------------------------------------------------: |
| 10BASE5      |  10 Мбит/с | Толстый коаксиальный кабель          |                                                   до 500 м |
| 10BASE2      |  10 Мбит/с | Тонкий коаксиальный кабель           |                                                   до 185 м |
| 10BASE-T     |  10 Мбит/с | Витая пара (обычно UTP)              |                                                   до 100 м |
| 10BASE-FL    |  10 Мбит/с | Оптоволокно                          |                до 2 км и более в зависимости от реализации |
| 100BASE-TX   | 100 Мбит/с | Витая пара Cat5/Cat5e                |                                                   до 100 м |
| 100BASE-T4   | 100 Мбит/с | Витая пара Cat3 и выше               |                                                   до 100 м |
| 100BASE-FX   | 100 Мбит/с | Многомодовое оптоволокно             |                                                    до 2 км |
| 1000BASE-T   |   1 Гбит/с | Витая пара Cat5e/Cat6                |                                                   до 100 м |
| 1000BASE-SX  |   1 Гбит/с | Многомодовое оптоволокно             |                                                  до ~550 м |
| 1000BASE-LX  |   1 Гбит/с | Одномодовое/многомодовое оптоволокно |                                                до ~5–10 км |
| 10GBASE-T    |  10 Гбит/с | Витая пара Cat6A/Cat7                |                                                   до 100 м |
| 10GBASE-SR   |  10 Гбит/с | Многомодовое оптоволокно             |                                              до ~300–400 м |
| 10GBASE-LR   |  10 Гбит/с | Одномодовое оптоволокно              |                                                  до ~10 км |
| 40GBASE-...  |  40 Гбит/с | В основном оптоволокно               |     зависит от варианта, обычно сотни метров или километры |
| 100GBASE-... | 100 Гбит/с | В основном оптоволокно               | зависит от варианта, обычно от сотен метров до десятков км |

## Формат Ethernet-кадра

![Формат Ethernet-кадра](images/02.%20Формат%20Ethernet-кадра.png)

Первые 6 байт — MAC-адрес получателя. Вторые 6 байт — MAC-адрес отправителя.

Следующие 2 байта — тип протокола вышестоящего уровня (предназначен, чтобы оборудование или операционная система могла понять, какой протокол инкапсулируется - `ARP`, `IPX`, `IPv4`, `IPv6`, `MPLS` и т.д. и т.п.).

Полезные данные в Ethernet обычно имеют длину от 46 до 1500 байт. Минимум в 46 байт нужен, чтобы кадр можно было эффективно обрабатывать в общей среде и чтобы избежать проблем с коллизиями, а максимум в 1500 байт не даёт одному кадру слишком долго занимать канал. В высокоскоростных сетях встречаются и более крупные jumbo frames, а иногда кадры бывают чуть меньше 46 байт из-за дополнительных заголовков, например VLAN или LLC.

Контрольная сумма нужна, чтобы выявлять повреждение данных при передаче: если пересчитанное значение у получателя не совпадает, кадр просто отбрасывается. При этом канальный уровень отвечает только за целостность в пределах своей сети и не занимается порядком доставки, подтверждениями, повторной передачей или сборкой сообщений — это задачи других уровней. Поэтому контрольная сумма не говорит ничего о возможных ошибках на других участках маршрута и не гарантирует целостность данных от отправителя до конечного получателя.

Как правило, когда рассматривают Ethernet-кадр, речь идет только о канальном уровне. На самом деле Ethernet-кадр содержит и структуры, относящиеся к физическому уровню: преамбулу (**Preambule**), ограничитель начала кадра (**Start of Frame Delimiter**) и межкадровый интервал (**Inter-Frame Gap**).

![Ethernet-кадр vs WIFI](images/02.%20Ethernet-кадра%20vs%20WIFI.png)

## CSMA/CD

Технология `Ethernet` базируется на методе управления доступом называемым **CSMA/CD** (Carrier Sense Multiply Access with Collision Detection) — это означает множественный доступ с контролем несущей и обнаружением коллизий. Коллизией называют столкновение информационных передач, таким образом, устройства в сети постоянно прослушивают эфир и останавливают любую передачу в случае детектирования коллизий.

## Cisco Packet Tracer

![Cisco Packet Tracer](images/02.%20Cisco%20Packet%20Tracer.png)

## Волоконно-оптические сети

Оптические волокна могут быть одномодовыми и многомодовыми.

Диаметр сердцевины **одномодовых волокон** составляет от 7 до 10 микрон. Благодаря столь малому диаметру достигается передача по волокну лишь одной моды излучения, за счет чего исключается влияние дисперсионных искажений.

**Многомодовые волокна** отличаются от одномодовых диаметром сердцевины, который составляет 50 микрон в европейском стандарте и 62,5 микрона в североамериканском и японском стандартах. Из-за большого диаметра сердцевины по многомодовому волокну распространяется несколько мод излучения — каждая под своим углом, из-за чего импульс света испытывает дисперсионные искажения и из прямоугольного превращается в колоколоподобный.

Многомодовые оптические кабели используются на расстояниях до 200 метров. Стоимость прокладки такой линии не намного дороже стоимости прокладки витой пары.

Одномодовые оптические кабели применяются, когда нужно организовать передачу данных на расстояние свыше 200 метров. Стоимость самого одномодового кабеля, как и стоимость соответствующего оборудования, в несколько раз дороже, чем стоимость многомодового кабеля.

При необходимости передавать данные на расстояния, измеряющиеся десятками километров, используются передатчики повышенной мощности.

Для подключения волоконно-оптических линий применяются так называемые **SFP-модули** (**Small Form-factor Pluggable**). Существуют и другие виды подобного оборудования, но `SFP-модули` (рис. 3.2) используются чаще всего— они подключаются в соответствующие порты активного оборудования.

![SFP-модули](images/02.%20SFP-модули.png)

Сами `SFP-модули` представляют собой компактные сменные приемопередатчики. В форм-факторе `SFP` выпускаются модули как для подключения витой пары, так и для оптических каналов.

Волоконно-оптические кабели требуют аккуратного обращения. Так, не допускается резко изгибать кабель, нужно следить за чистотой оптических поверхностей (не трогать их руками, всегда закрывать концы кабеля и гнезда специальными заглушками и т. п.). В случае повреждения кабеля придется воспользоваться услугами

## Питание по сети Ethernet (РоЕ)

Некоторое современное оборудование может получать питание по технологии питания оборудования по кабелю `Ethernet` (**Power over Ethernet, РоЕ**). Как правило, запитывают по РоЕ точки беспроводного доступа, камеры видеонаблюдения, IP-телефоны и прочее не очень мощное оборудование.

Технология `РоЕ` основана на том, что в стандартах передачи данных 10/100 Мбит/с задействованы только две пары проводов витой пары из четырех имеющихся, а остальные две можно использовать для питания некоторого не очень мощного оборудования.

Обычно для работы этой технологии устанавливаются специальные `РоЕ-коммутаторы`. При этом допускается установка дополнительного блока питания, который будет запитывать `РоЕ-устройства`. Администраторы могут использовать управление портами `РоЕ-коммутатора` для перезагрузки зависшего устройства — для этого просто нужно снять напряжение с соответствующего порта, а затем заново его подать.

![PoE](images/02.%20PoE.jpg)
